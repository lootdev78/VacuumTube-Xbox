<Page
    x:Class="VacuumTube.Xbox.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VacuumTube.Xbox"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="#282828">
    <Grid>
        <muxc:WebView2 x:Name="Browser" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <Border x:Name="StartupOverlay" Background="#282828">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="18" MaxWidth="720">
                <Image Source="Assets/StoreLogo.png" Width="128" Height="128" />
                <TextBlock Text="VacuumTube" Foreground="White" FontSize="34" HorizontalAlignment="Center" />
                <TextBlock x:Name="StartupStatus"
                           Text="WebView2 wird initialisiert …"
                           Foreground="White"
                           FontSize="20"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center" />
                <ProgressRing x:Name="StartupProgress" IsActive="True" Width="48" Height="48" />
                <Button x:Name="RetryButton"
                        Content="Erneut versuchen"
                        Click="OnRetryClicked"
                        Visibility="Collapsed"
                        HorizontalAlignment="Center"
                        Padding="24,10" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
